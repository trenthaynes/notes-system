"""Notes CLI entry point (skeleton).

Commands will be added in later units.
"""

import typer

app = typer.Typer(name="notes", no_args_is_help=True, help="Personal notes management.")

# Commands will be added in later units
